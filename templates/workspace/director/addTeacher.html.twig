{% extends 'workspace/workspaceBase.html.twig' %}

{% block main %}

    <section id="register">
        <div class="container px-4">
            <div class="row gx-4 justify-content-center">
                <div class="col-lg-8">
                    <h2>Dodaj nauczyciela</h2>
                    <p class="lead">Jeśli chcesz dodać swojego nauczyciela do organizacji, proszę podaj nam jego adres email.</p>
                    <div class="col-8 mx-auto mt-5">
                        {{ form_start(form,{'attr':{'class':'form-material m-t-40'}}) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </section>

{% endblock %}