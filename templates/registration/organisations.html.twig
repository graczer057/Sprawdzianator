{% extends 'landing/base.html.twig' %}

{% block main %}

    {% if app.user %}
    <section id="register">
        <div class="container px-4">
            <div class="row gx-4 justify-content-center">
                <div class="col-lg-8">
                    <h2>Rejestracja organizacji</h2>
                    <p class="lead">Widzimy, że jeszcze nie należysz do żadnej organizacji. Żeby poprawnie korzystać z serwisu, musisz nam podać nazwę swojej organizacji. Tylko Ty możesz utworzyć nową przestrzeń (jedna przestrzeń przysługuje jednej szkole). Zarządcą twojej przestrzeni może być tylko dyrektor placówki dlatego, jeśli jesteś nauczycielem w kolejnym kroku poprosimy Ciebie o podanie adresu email dyrektora twojej placówki.</p>
                    <div class="col-8 mx-auto mt-5">
                        {{ form_start(form,{'attr':{'class':'form-material m-t-40'}}) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
    {% else %}
    <section id="register">
        <div class="container px-4">
            <div class="row gx-4 justify-content-center">
                <div class="col-lg-8">
                    <h2>Oops... wygląda na to, że ta strona przysługuje tylko zalogowanym użytkownikom. Wróć tutaj jak już będziesz zalogowany/a</h2>
                </div>
            </div>
        </div>
    </section>
    {% endif %}

{% endblock %}