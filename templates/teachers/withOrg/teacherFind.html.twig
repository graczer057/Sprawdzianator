{% extends 'landing/base.html.twig' %}

{% block main %}
    <section id="register">
        <div class="container px-4">
            <div class="row gx-4 justify-content-center">
                <div class="col-lg-8">
                    <h2>Wyszukaj swoją organizację</h2>
                    <p class="lead">Wyszukaj swoją organizację spośród istniejących. Jeśli nie widzisz na liście swojej organizacji to znaczy, że jeszcze nie została utworzona. Tylko dyrektorowie placówek mają taką możliwość.</p>
                    <div class="col-8 mx-auto mt-5">
                        {{ form_start(form,{'attr':{'class':'form-material m-t-40'}}) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}